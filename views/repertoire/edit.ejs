<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>New Song</title>
</head>
<body>
    <%- include('../partials/navbar.ejs') %>
    <div class="edit-body">
        <form action="/users/<%= user._id %>/repertoire/<%= song._id %>?_method=PUT" method="POST">
           <label for="title" >Title</label> 
           <input type="text" name="title" id="title" value="<%=song.title %>">
           <label for="author" >Author</label> 
           <input type="text" name="author" id="author" value="<%=song.author %>">
           <label for="category">Category</label>
           <select name="category" id="category">
                <option value="entrance"
                <%= song.category === 'entrance' ? 'selected' : '' %>
                >
                    Entrance
                </option>
                <option value="offertory"
                    <%= song.category === 'offertory' ? 'selected' : '' %>
                >
                    Offertory
                </option>
                <option value="communion"
                    <%= song.category === 'communion' ? 'selected' : '' %>
                >
                    Communion
                </option>
                <option value="exit"
                    <%= song.category === 'exit' ? 'selected' : '' %>
                >
                    Exit
                </option>
                
           </select>
           
           <label for="songBook">SongBook</label>
           <input type="text" name="songBook" id="songBook" value="<%=song.songBook %>">
           <label for="page">Page Number</label>
           <input type="number" name="page" id="page" value="<%=song.page %>">
           <button type="submit">Edit</button>
        </form>
    </div>
</body>
</html>